import { Location } from '../types';

// Mock locations removed — all data is served from Supabase.
// This file is kept as a fallback stub; useLocations.ts falls back here
// when EXPO_PUBLIC_SUPABASE_URL is not set.
export const MOCK_LOCATIONS: Location[] = [];
